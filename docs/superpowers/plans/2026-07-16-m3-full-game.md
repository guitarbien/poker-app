# M3 完整對戰 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把最小牌桌升級為完整對戰：三種 CPU 難度、練習輔助顯示（勝率/賠率/成牌，可開關）、開局設定、手間補碼、戰績與手牌記錄（localStorage）、PWA 離線。

**Architecture:** 延續「純邏輯模組 + 薄 UI」：storage（版本化 localStorage 封裝，Storage 可注入）、cpu（三難度分派，normal 用 RFI 表 + 賠率、hard 用 equity）、review/recorder（HandRecord 組裝為純函式，副作用集中在 useTable 的單一 useEffect）、stats（純 counter 累計）。先還 M2 技術債（reducer 純化）再疊功能。

**Tech Stack:** 既有 + `vite-plugin-pwa`（M3 唯一新增依賴）。

**Spec:** `docs/superpowers/specs/2026-07-14-holdem-practice-design.md`（§4 HandRecord/stats、§6 CPU、§7 UI、§13 M3）

## Global Constraints

- **TDD**：純邏輯（Task 1–5）先寫失敗測試；UI（Task 6–7）以 typecheck + 瀏覽器實測 + e2e 驗證
- **每個 commit 前**：`npm run typecheck && npm run test` 全綠；邏輯任務另跑 `npm run coverage`
- **覆蓋率**：coverage include 擴為既有 + `'src/storage/**/*.ts'`、`'src/review/**/*.ts'`、`'src/stats/**/*.ts'`，thresholds 維持 85
- **引擎凍結**：`src/engine/` 只允許 Task 1 的「匯出具名 LegalActions 型別」（型別重構，不改行為）
- **新依賴僅限** `vite-plugin-pwa`（Task 8）
- **e2e 埠**：5199（dev）／5200（preview，PWA 測試）；絕不用 5173
- **localStorage keys**（spec §4）：`holdem.settings`、`holdem.stats`、`holdem.hands`（環形 500）；一律經 storage 模組存取，皆含 version
- **遊戲參數**：盲注等級 1/2（預設）、2/5、5/10；買入 100 BB；CPU 數 1–5
- 介面繁體中文；commit 繁體中文；POSIX 檔尾換行；engine/cpu 的 rng 注入、UI 層用 Math.random

---

### Task 1: M2 技術債重構（reducer 純化等）

**Files:**
- Modify: `src/engine/game.ts`（只加具名型別）、`src/features/table/useTable.ts`、`src/features/table/ActionBar.tsx`、`src/features/table/session.ts`、`vitest.config.ts`
- Test: 既有測試全綠 + e2e 全綠即驗收（行為不變的重構）

**Interfaces:**
- Produces:
  - `engine/game.ts`：`export interface LegalActions { fold: boolean; check: boolean; call: { amount: number } | null; raise: { min: number; max: number } | null }`，`legalActions(state): LegalActions`；`ActionBar` 改 `import type { LegalActions }`
  - `useTable.ts`：`TableEvent` 的 `start`/`nextHand`/`humanRebuyAndNext` 增加 `deck: Card[]` 欄位；reducer 內不再呼叫 `shuffle`/`Math.random`（純函式）；dispatch 端（`useTable` 回傳的包裝函式或元件呼叫處）負責 `shuffle(createDeck(), rng)`。對外提供便捷 API：

```ts
export interface TableApi {
  state: TableState;
  start: (config: SessionConfig) => void;      // 內部洗牌後 dispatch
  act: (action: Action) => void;               // humanAction
  nextHand: () => void;
  rebuyAndNext: () => void;                    // 破產重買「與」手間補碼共用
  exit: () => void;
}
export function useTable(): TableApi;
```

  - **`humanRebuyAndNext` 的 guard 放寬**：原 `!state.humanBusted` 會吞掉非破產的手間補碼。改為「`street === 'handOver'` 且 human.stack < 買入」即可執行（涵蓋破產與補碼兩情境；補碼金額由 `session.humanRebuy` 的差額邏輯處理）

  - `session.ts`：`humanRebuy` 補碼金額 `Math.max(0, buyIn - human.stack)`，金額為 0 時直接回傳原 state（不呼叫 rebuy）
  - `vitest.config.ts`：`exclude: [...configDefaults.exclude, 'e2e/**']`（`import { configDefaults } from 'vitest/config'`）

- [ ] **Step 1**: 依上述重構（CPU 的 `cpuAction` dispatch 留在 useEffect 內，不需 deck）
- [ ] **Step 2**: TableScreen/HomeScreen 改用 `TableApi`（呼叫處等價替換）
- [ ] **Step 3**: `npm run typecheck && npm run test && npm run test:e2e` 全綠（重構不改行為，e2e 是行為鎖）
- [ ] **Step 4**: Commit：`重構：reducer 純化與 LegalActions 具名型別（M2 技術債）`

---

### Task 2: storage.ts（版本化 localStorage 封裝）

**Files:**
- Create: `src/storage/storage.ts`
- Test: `src/storage/storage.test.ts`
- Modify: `vitest.config.ts`（coverage include 加 `'src/storage/**/*.ts'`）

**Interfaces:**
- Produces:

```ts
// 可注入 Storage（測試用 in-memory fake；預設 window.localStorage）
export interface KVStore {
  getItem(key: string): string | null;
  setItem(key: string, value: string): void;
  removeItem(key: string): void;
}

export function load<T>(key: string, version: number, fallback: T, store?: KVStore): T;
// 讀不到 / JSON 壞 / version 不符 → 回 fallback（version 不符時 console.warn）

export function save<T>(key: string, version: number, data: T, store?: KVStore): boolean;
// 成功 true；QuotaExceeded 等寫入失敗 → false（呼叫端決定淘汰重試）
```

儲存格式：`JSON.stringify({ version, data })`。

- [ ] **Step 1: 寫失敗測試**

```ts
import { describe, it, expect, vi } from 'vitest';
import { load, save, type KVStore } from './storage';

function memStore(init: Record<string, string> = {}): KVStore {
  const m = new Map(Object.entries(init));
  return {
    getItem: (k) => m.get(k) ?? null,
    setItem: (k, v) => void m.set(k, v),
    removeItem: (k) => void m.delete(k),
  };
}

describe('storage', () => {
  it('save 後 load 取回原資料', () => {
    const s = memStore();
    expect(save('k', 1, { a: 1 }, s)).toBe(true);
    expect(load('k', 1, { a: 0 }, s)).toEqual({ a: 1 });
  });

  it('不存在 / JSON 壞掉 / version 不符 → 回 fallback', () => {
    const s = memStore({ bad: '{oops', old: JSON.stringify({ version: 1, data: 5 }) });
    expect(load('none', 1, 'fb', s)).toBe('fb');
    expect(load('bad', 1, 'fb', s)).toBe('fb');
    expect(load('old', 2, 'fb', s)).toBe('fb'); // 版本不符
  });

  it('setItem 丟例外（quota）→ save 回傳 false', () => {
    const s = memStore();
    s.setItem = vi.fn(() => { throw new Error('QuotaExceededError'); });
    expect(save('k', 1, 'x', s)).toBe(false);
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**
- [ ] **Step 3: 實作**

```ts
export interface KVStore {
  getItem(key: string): string | null;
  setItem(key: string, value: string): void;
  removeItem(key: string): void;
}

function defaultStore(): KVStore {
  return window.localStorage;
}

export function load<T>(key: string, version: number, fallback: T, store: KVStore = defaultStore()): T {
  try {
    const raw = store.getItem(key);
    if (raw === null) return fallback;
    const parsed = JSON.parse(raw) as { version: number; data: T };
    if (parsed.version !== version) {
      console.warn(`[storage] ${key} 版本不符（${parsed.version} ≠ ${version}），以預設值重建`);
      return fallback;
    }
    return parsed.data;
  } catch (err) {
    console.warn(`[storage] 讀取 ${key} 失敗，以預設值重建`, err);
    return fallback;
  }
}

export function save<T>(key: string, version: number, data: T, store: KVStore = defaultStore()): boolean {
  try {
    store.setItem(key, JSON.stringify({ version, data }));
    return true;
  } catch (err) {
    console.warn(`[storage] 寫入 ${key} 失敗`, err);
    return false;
  }
}
```

- [ ] **Step 4**: coverage include 更新；`npm run typecheck && npm run test && npm run coverage` 全綠
- [ ] **Step 5**: Commit：`新增版本化 localStorage 封裝`

---

### Task 3: normal / hard CPU 難度

**Files:**
- Modify: `src/cpu/strategy.ts`
- Test: `src/cpu/strategy.test.ts`（追加）

**前置注意**：①M2 的 easy 邏輯目前內聯在 `decideAction` 本體——先把它抽出成 `decideEasy(state, seat, rng)`（行為不變），`decideAction` 才改為分派器；②本任務程式碼使用具名型別 `LegalActions`，**依賴 Task 1 先完成**，不可跳過 Task 1 單獨實作。

**Interfaces:**
- Consumes: `engine/ranges`（`RFI_RANGES`、`isInRange`、`handClassOf`、`parseRangeString`）、`engine/equity`（`equity`）、`engine/game`（`positionOf`）
- Produces: `decideAction` 依 `difficulty` 分派到 easy/normal/hard；三者皆只回傳合法動作

決策規則（spec §6；数值為本計畫定案）：

- **normal（表驅動 + 賠率）**
  - preflop 無人加注（`currentBet === bb`）：BB 可過牌就過牌；其餘位置 `isInRange(RFI_RANGES[pos])` → open 至 2.5 BB（夾制），否則棄牌（有 limper 時同樣照表 raise，簡化註明）
  - preflop 面對加注：`QQ+, AKs, AKo` → 80% 3-bet 至 3× currentBet、否則跟注；`66+, ATs+, KTs+, QTs+, JTs, AJo+, KQo` → 跟注；其餘棄牌（可過牌則過牌）
  - postflop：兩對以上 → 70% 加注 2/3 池否則跟/過；一對 → 可過牌就過牌（35% 改為下注 1/2 池）、面對注時所需勝率 ≤ 0.30 才跟；高牌 → 過牌/棄牌
- **hard（equity 驅動）**
  - preflop 與 normal 相同（表驅動已足夠，差異在 postflop；程式碼直接重用 normal 的 preflop 分支）
  - postflop：`eq = equity(hole, board, 未棄牌對手數, 500, rng)`、`needed = owe / (pot + owe)`
    - 可過牌時：eq > 0.65 → 下注 2/3 池；0.28 < eq < 0.45 且 35% → 半詐唬下注 1/2 池；否則過牌
    - 面對下注：eq > 0.70 且 50% → 加注約 1 池；`eq ≥ needed + 0.03` → 跟注；eq > 0.30 且 12% → 半詐唬加注 2/3 池；否則棄牌

- [ ] **Step 1: 追加失敗測試**（import 併頂部）

```ts
describe('decideAction 難度分派與合法性', () => {
  it('normal 與 hard 各 60 手全 CPU 對局：決策皆合法且終止', () => {
    for (const difficulty of ['normal', 'hard'] as const) {
      const rng = mulberry32(difficulty === 'normal' ? 33 : 44);
      let stacks = [200, 200, 200, 200, 200, 200];
      let button = 0;
      for (let hand = 1; hand <= 60; hand++) {
        let s = newHand({
          players: [0, 1, 2, 3, 4, 5].map((seat) => ({ seat, stack: stacks[seat], isCpu: true })),
          button, blinds: { sb: 1, bb: 2 }, handNumber: hand,
          deck: shuffle(createDeck(), rng),
        });
        let steps = 0;
        while (s.street !== 'handOver') {
          s = applyAction(s, decideAction(s, s.toAct!, difficulty, rng));
          expect(++steps).toBeLessThan(2000);
        }
        for (const p of s.players) if (p.stack === 0) s = rebuy(s, p.seat, 200);
        stacks = [0, 1, 2, 3, 4, 5].map((seat) => s.players.find((p) => p.seat === seat)!.stack);
        button = nextButton(s);
      }
    }
  }, 60_000);
});

describe('normal 行為錨點', () => {
  it('UTG 照表開牌：AJo 加注 2.5BB、ATo 棄牌、72o 棄牌', () => {
    // 無 rng 分支：open/fold 是決定性的
    expect(decideAction(utgWith(['As', 'Jd']), 3, 'normal', mulberry32(1)))
      .toEqual({ type: 'raise', to: 5 });
    expect(decideAction(utgWith(['As', 'Td']), 3, 'normal', mulberry32(1)).type).toBe('fold');
    expect(decideAction(utgWith(['7s', '2d']), 3, 'normal', mulberry32(1)).type).toBe('fold');
  });

  it('面對加注拿 AA 絕不棄牌', () => {
    // seat3 open 後輪 seat4；rig seat3=KQo、seat4=AA
    let s = utgWith2(['Ks', 'Qd'], ['Ah', 'Ad']);
    s = applyAction(s, { type: 'raise', to: 6 }); // seat3 open（測試手動代打）
    for (let i = 0; i < 100; i++) {
      expect(decideAction(s, 4, 'normal', mulberry32(i)).type).not.toBe('fold');
    }
  });
});

describe('hard 行為錨點', () => {
  it('河牌坐擁堅果面對注絕不棄牌', () => {
    // HU rig：CPU seat1 拿 9c9s、board 9h 9d 5c 2s 7h（四條九，不可能被反超）
    let s = huRigged(['2c', '3d'], ['9c', '9s'], ['9h', '9d', '5c', '2s', '7h']);
    // 打到河牌：preflop call/check、flop check/check、turn check/check、river 人類下注
    s = applyAction(s, { type: 'call' });   // preflop：seat1（button/SB）補齊
    s = applyAction(s, { type: 'check' });  // seat0（BB）→ flop
    for (let i = 0; i < 2; i++) { s = applyAction(s, { type: 'check' }); s = applyAction(s, { type: 'check' }); } // flop、turn 全過牌
    s = applyAction(s, { type: 'raise', to: 10 }); // river：seat0（BB 先行動）下注
    for (let i = 0; i < 60; i++) {
      expect(decideAction(s, 1, 'hard', mulberry32(i)).type).not.toBe('fold');
    }
  });

  it('河牌垃圾牌面對大注以棄牌為主', () => {
    // CPU 拿 2c3d 高牌、board 無連結，面對滿池注
    let s = huRigged(['Ah', 'Kh'], ['2c', '3d'], ['Ts', 'Jd', '6h', 'Qc', '8s']);
    s = applyAction(s, { type: 'call' });
    s = applyAction(s, { type: 'check' });
    for (let i = 0; i < 2; i++) { s = applyAction(s, { type: 'check' }); s = applyAction(s, { type: 'check' }); }
    s = applyAction(s, { type: 'raise', to: 8 }); // river：seat0 下滿池注
    let folds = 0;
    for (let i = 0; i < 100; i++) {
      if (decideAction(s, 1, 'hard', mulberry32(i)).type === 'fold') folds++;
    }
    expect(folds).toBeGreaterThan(70);
  });
});
```

（`utgWith2(holeSeat3, holeSeat4)` 與 `huRigged(holeSeat0, holeSeat1, board)` 為測試 helper，比照既有 `utgWith`/`riggedDeck` 寫法：依發牌順序組 deck——6 人桌 button 0 發牌序 seat1..5,0；HU button 1 發牌序 seat0, seat1。實作時放測試檔頂部 helper 區。）

- [ ] **Step 2: 跑測試確認失敗**
- [ ] **Step 3: 實作**（完整程式碼）

```ts
// 追加於 strategy.ts；既有 decideEasy/roughStrength/easyRaiseTo 不動
import { RFI_RANGES, isInRange, handClassOf, parseRangeString } from '../engine/ranges';
import { positionOf } from '../engine/game';
import { equity } from '../engine/equity';

const THREEBET_RANGE = parseRangeString('QQ+, AKs, AKo');
const CALL_RAISE_RANGE = parseRangeString('66+, ATs+, KTs+, QTs+, JTs, AJo+, KQo');

function potSize(state: GameState): number {
  return state.players.reduce((s, p) => s + p.totalCommitted, 0);
}

function oweOf(state: GameState, seat: number): number {
  const p = state.players.find((x) => x.seat === seat)!;
  return Math.max(0, state.currentBet - p.committed);
}

function neededEquity(state: GameState, seat: number): number {
  const owe = oweOf(state, seat);
  if (owe === 0) return 0;
  return owe / (potSize(state) + owe);
}

function clampTo(la: LegalActions, to: number): number {
  return Math.max(la.raise!.min, Math.min(la.raise!.max, Math.round(to)));
}

function betFraction(state: GameState, la: LegalActions, fraction: number): Action {
  return { type: 'raise', to: clampTo(la, state.currentBet + potSize(state) * fraction) };
}

function preflopByTable(state: GameState, seat: number, rng: Rng): Action {
  const la = legalActions(state);
  const p = state.players.find((x) => x.seat === seat)!;
  const [a, b] = p.hole!;
  const unopened = state.currentBet === state.blinds.bb;
  if (unopened) {
    if (la.check) return { type: 'check' }; // BB 免費看牌
    const pos = positionOf(state, seat);
    if (pos !== 'BB' && isInRange(RFI_RANGES[pos], a, b) && la.raise) {
      return { type: 'raise', to: clampTo(la, 2.5 * state.blinds.bb) };
    }
    return { type: 'fold' };
  }
  const hc = handClassOf(a, b);
  if (THREEBET_RANGE.has(hc)) {
    if (la.raise && rng() < 0.8) return { type: 'raise', to: clampTo(la, 3 * state.currentBet) };
    if (la.call) return { type: 'call' };
  }
  if (CALL_RAISE_RANGE.has(hc) && la.call) return { type: 'call' };
  if (la.check) return { type: 'check' };
  return { type: 'fold' };
}

function decideNormal(state: GameState, seat: number, rng: Rng): Action {
  if (state.street === 'preflop') return preflopByTable(state, seat, rng);
  const la = legalActions(state);
  const p = state.players.find((x) => x.seat === seat)!;
  const category = decodeScore(bestHand([...p.hole!, ...state.board]).score).category;
  if (category >= 2) {
    if (la.raise && rng() < 0.7) return betFraction(state, la, 2 / 3);
    if (la.call) return { type: 'call' };
    if (la.check) return { type: 'check' };
    return { type: 'fold' };
  }
  if (category === 1) {
    if (la.check) {
      if (la.raise && rng() < 0.35) return betFraction(state, la, 0.5);
      return { type: 'check' };
    }
    if (la.call && neededEquity(state, seat) <= 0.30) return { type: 'call' };
    return { type: 'fold' };
  }
  if (la.check) return { type: 'check' };
  return { type: 'fold' };
}

function decideHard(state: GameState, seat: number, rng: Rng): Action {
  if (state.street === 'preflop') return preflopByTable(state, seat, rng);
  const la = legalActions(state);
  const p = state.players.find((x) => x.seat === seat)!;
  const opponents = state.players.filter((x) => x.seat !== seat && x.state !== 'folded').length;
  const eq = equity(p.hole!, state.board, opponents, 500, rng);
  const needed = neededEquity(state, seat);
  if (la.check) {
    if (eq > 0.65 && la.raise) return betFraction(state, la, 2 / 3);
    if (eq > 0.28 && eq < 0.45 && la.raise && rng() < 0.35) return betFraction(state, la, 0.5);
    return { type: 'check' };
  }
  if (eq > 0.70 && la.raise && rng() < 0.5) return betFraction(state, la, 1);
  if (eq >= needed + 0.03 && la.call) return { type: 'call' };
  if (eq > 0.30 && la.raise && rng() < 0.12) return betFraction(state, la, 2 / 3);
  return { type: 'fold' };
}

export function decideAction(state: GameState, seat: number, difficulty: Difficulty, rng: Rng): Action {
  if (difficulty === 'normal') return decideNormal(state, seat, rng);
  if (difficulty === 'hard') return decideHard(state, seat, rng);
  return decideEasy(state, seat, rng);
}
```

（既有 `decideAction` 改為以上分派版；`_difficulty` 參數改名 `difficulty`。normal 的 AJo open to 5 = 2.5×2 夾制後仍 5：`la.raise.min` preflop 為 4、max 200，round(5)=5 ✓ 與錨點測試一致。）

- [ ] **Step 4**: `npm run typecheck && npm run test && npm run coverage` 全綠
- [ ] **Step 5**: Commit：`新增普通與困難 CPU 難度（開牌表與勝率驅動）`

---

### Task 4: recorder.ts（HandRecord 組裝與持久化）

**Files:**
- Create: `src/review/recorder.ts`
- Test: `src/review/recorder.test.ts`
- Modify: `src/features/table/useTable.ts`（reducer 累積 log、useEffect 持久化）、`vitest.config.ts`（include 加 `'src/review/**/*.ts'`）

**Interfaces:**
- Produces:

```ts
// spec §4 的 HandRecord
export interface HandRecord {
  version: number;          // 1
  handNumber: number;
  timestamp: number;
  blinds: { sb: number; bb: number };
  button: number;
  players: { seat: number; stack: number; hole: [Card, Card]; isCpu: boolean }[]; // stack = 貼盲前
  actions: { seat: number; street: string; action: Action; potAfter: number }[];
  board: Card[];
  potResults: PotResult[];
  humanNet: number;         // 人類本手淨損益 = 結束 stack − 起始 stack（Task 5 戰績依賴）
  flags: ReviewFlag[];      // M3 一律 []；M4 填入
}
export interface ReviewFlag {
  actionIndex: number;
  kind: 'preflop-loose' | 'preflop-tight' | 'call-without-odds';
  detail?: { requiredEquity?: number; estimatedEquity?: number };
}

export interface HandLog {
  startPlayers: HandRecord['players'];   // newHand 前的 config 快照（stack 含盲注前）
  blinds: { sb: number; bb: number };
  button: number;
  handNumber: number;
  entries: HandRecord['actions'];
}

export function buildHandRecord(log: HandLog, final: GameState, timestamp: number): HandRecord;

export const HANDS_KEY = 'holdem.hands';
export const HANDS_VERSION = 1;
export const HANDS_LIMIT = 500;
// 環形緩衝：滿了淘汰最舊；save 失敗（quota）→ 砍半後重試一次
export function appendHand(record: HandRecord, store?: KVStore): void;
export function loadHands(store?: KVStore): HandRecord[];
```

**useTable 接線**（Task 1 之後 reducer 已純化）：
- `TableState` 加 `handLog: HandLog | null`
- `start`/`nextHand`/`humanRebuyAndNext` case：建立 `handLog`（`startPlayers` 從 `NewHandConfig.players` 映射 `hole`＝發牌後的 `game.players[].hole`——即 newHand 之後從 game state 取 hole、stack 用 config 的貼盲前值）
- `humanAction`/`cpuAction` case：applyAction 成功後記錄 `{ seat: 行動前的 toAct, street: 行動前的 street, action, potAfter: Σ totalCommitted }`。**必須 immutable append（`entries: [...entries, entry]`），禁止 mutation push**——StrictMode 會以同一 (state, event) 重複呼叫 reducer，mutation 會重複記錄。加一條測試：同一 event 呼叫 reducer 兩次，結果相等且 entries 無重複（reducer 是純函式可直接單元測試）
- `useTable` 加一個 useEffect：`game?.street === 'handOver' && handLog` 且未持久化 → `buildHandRecord(handLog, game, Date.now())` → `appendHand` → 同步更新 stats（Task 5 的 `recordHand`）。**防重 ref 不可用 handNumber**（exit 後重新 start，handNumber 會重複）：`useRef<HandLog | null>` 記「已持久化的 handLog 物件參考」，比對參考不等才寫入；`start`/`exit` 時重置為 null

- [ ] **Step 1: 寫失敗測試**（buildHandRecord 用手動組的 log+final 驗欄位；appendHand 用 memStore 驗環形淘汰與 quota 砍半重試——502 筆後長度 500 且最舊被淘汰；quota fake：前 N 次 setItem 丟例外）
- [ ] **Step 2: 確認失敗 → Step 3: 實作 → Step 4: 全綠 + coverage**
- [ ] **Step 5**: Commit：`新增手牌記錄：HandRecord 組裝與環形持久化`

---

### Task 5: stats.ts（戰績 counters）

**Files:**
- Create: `src/stats/stats.ts`
- Test: `src/stats/stats.test.ts`
- Modify: `src/features/table/useTable.ts`（handOver effect 一併更新）、`vitest.config.ts`（include 加 `'src/stats/**/*.ts'`）

**Interfaces:**

```ts
export interface Stats {
  handsPlayed: number;
  totalWinnings: number;   // Σ(分得彩池 − 投入)，含盲注
  vpipHands: number;       // preflop 主動投入（call/raise；貼盲與 BB 免費看牌不算）
  pfrHands: number;        // preflop 加注過
  showdownsSeen: number;   // 玩家未棄牌且進入攤牌（record.potResults 有 handRank 非 null 且玩家未棄牌）
  showdownsWon: number;    // 攤牌且分得彩池
}
export const EMPTY_STATS: Stats;
export const STATS_KEY = 'holdem.stats';
export const STATS_VERSION = 1;
// 從一筆 HandRecord 累計人類（seat 0）的戰績；純函式
export function recordHand(prev: Stats, record: HandRecord): Stats;
```

判定細節（測試錨點）：
- `totalWinnings` 增量 = Σ（`potResults` 中 seat 0 的 `amount`）−（seat 0 的總投入 = 從 `actions` 累計 seat 0 的 call/raise 投入 + 盲注——**簡化：直接用 `record.players` 起始 stack 無法推得投入，故 HandRecord 的 `actions.potAfter` 差分也繁瑣；改為在 HandRecord 增加 `netWinnings: number[]`？不——最簡：`buildHandRecord` 已有 `final: GameState`，直接在 HandRecord 加一個 `humanNet: number`（= final.stack − start.stack，正確涵蓋所有投入與贏得）**。Task 4 的 HandRecord 介面加上 `humanNet: number` 欄位（buildHandRecord 計算），本任務直接使用
- `vpip`/`pfr`：掃 `record.actions` 中 seat 0 且 street === 'preflop' 的動作：有 call 或 raise → vpip+1；有 raise → pfr+1
- `showdownsSeen`：`potResults` 任一 winners 有 `handRank !== null`（發生攤牌）且 seat 0 在 `actions` 中沒有 fold → +1
- `showdownsWon`：發生攤牌且 seat 0 出現在任一 `potResults[].winners` → +1

- [ ] **Step 1: 寫失敗測試**（手組三筆 HandRecord：①seat0 preflop fold（vpip 不變、winnings 負盲注）②seat0 raise 進攤牌獲勝 ③seat0 limp 後 flop fold）
- [ ] **Step 2–4: TDD 循環 + coverage 全綠**
- [ ] **Step 5**: Commit：`新增戰績統計：VPIP、PFR 與攤牌勝率 counters`

---

### Task 6: 練習輔助顯示 + 牌局流程補完

**Files:**
- Create: `src/features/table/AssistPanel.tsx`（+ module.css）
- Create: `src/features/home/settings.ts`（**在本任務建立**，Task 7 只擴充 UI）＋ `src/features/home/settings.test.ts`
- Modify: `TableScreen.tsx`、`useTable.ts`（如需）、`src/features/table/handNames.ts`（擴充細節格式）、`vitest.config.ts`（coverage include 加 `'src/features/home/settings.ts'`）

**settings.ts（v1 完整 schema 一次定案，避免部分寫入的 schema 洞）：**

```ts
export interface Settings {
  blinds: { sb: number; bb: number };
  cpuCount: number;              // 1–5
  difficulties: Difficulty[];    // 長度 = cpuCount
  assistEnabled: boolean;
}
export const DEFAULT_SETTINGS: Settings; // 1/2、5 CPU、全 easy、assist 開
export function loadSettings(store?: KVStore): Settings;
// 讀取後一律 { ...DEFAULT_SETTINGS, ...data } merge-normalize（缺欄位補預設），
// difficulties 長度與 cpuCount 不一致時 normalize（裁切/補 easy）——不能只靠 version
export function saveSettings(s: Settings, store?: KVStore): void;
```

（settings.ts 以 TDD 開發：load 缺欄位補預設、difficulties normalize、save/load roundtrip。）

**規格（spec §7）：**
- **AssistPanel**（可整體開關，預設開，開關經 `loadSettings`/`saveSettings` 持久化）：
  - 我方即時勝率：`equity(hole, board, 活躍對手數, 2000, Math.random)`，「vs 隨機手牌」標註；**只在街道改變或對手棄牌時重算**（`useMemo` deps：`[game?.handNumber, game?.street, activeOpponents]`），下注動作不觸發
  - 底池賠率：輪到玩家且 owe > 0 時顯示「跟注 N ／ 底池 M ＝ 需要 X% 勝率」
  - 目前成牌：postflop 顯示**含 rank 細節**的名稱（spec §7 範例「兩對：K 和 7」）——擴充 handNames.ts：`describeHand(score): string`（用 `decodeScore().tiebreakers` 組出「兩對：K 和 7」「一對：A」「順子：9 高」等格式，rank 以 2–9/T/J/Q/K/A 顯示），附單元測試；preflop 顯示手牌類（`handClassOf`）
- **牌局流程**：結果橫幅顯示後 **5 秒自動進下一手**（倒數顯示，點「下一手」立即；humanBusted 時不倒數）；handOver 且 `0 < stack < 買入` 顯示「補碼」鈕 = 補滿後直接開下一手（走 Task 1 放寬後的 `rebuyAndNext`；實測驗收須含「非破產補碼後開新手且籌碼補滿」）
- **離開牌桌**：header 常駐「離開」鈕（回首頁；牌局中離開直接棄局不記錄該手）
- 開關與倒數的行為以 e2e/實測驗證

- [ ] **Step 1**: 實作 AssistPanel 與流程補完
- [ ] **Step 2**: `npm run typecheck && npm run test` 全綠；瀏覽器實測（桌機+375×667）：輔助資訊三項顯示正確、開關生效且重整後記住、自動下一手倒數、補碼鈕、離開鈕
- [ ] **Step 3**: Commit：`新增練習輔助顯示與牌局流程補完`

---

### Task 7: 開局設定

**Files:**
- Modify: `src/features/home/HomeScreen.tsx`（+ css）、`src/App.tsx`（傳遞 config）、`src/features/table/session.ts`（+測試）
- （`settings.ts` 已在 Task 6 建立，本任務只消費）

**規格：**
- 設定項：盲注等級（1/2、2/5、5/10 三選一）、CPU 數量（1–5）、難度（「全部」快捷：easy/normal/hard；或逐一設定每個 CPU）
- 「開始牌局」用當前設定開桌（`SessionConfig` 擴為 `difficulties: Difficulty[]`——session.ts 的 `initialHandConfig` 改吃陣列，per-CPU 難度；既有 session/useTable 呼叫端與測試同步微調）
- **HomeScreen 改版必須保留 role=button、accessible name「開始牌局」**（既有 e2e 與 Task 8 offline spec 依賴）

- [ ] **Step 1**: session.ts 擴充 difficulties 陣列（TDD：per-CPU 難度正確落到 players[].difficulty）
- [ ] **Step 2**: UI 實作（設定表單 + 持久化）
- [ ] **Step 3**: `npm run typecheck && npm run test && npm run coverage && npm run test:e2e` 全綠；瀏覽器實測：改設定（2 個 hard CPU、盲注 2/5）開桌生效、重整記住設定
- [ ] **Step 4**: Commit：`新增開局設定：盲注、CPU 數量與難度`

---

### Task 8: PWA 離線 + e2e 擴充 + M3 驗收

**Files:**
- Modify: `vite.config.ts`（vite-plugin-pwa）、`package.json`（devDependency）、`index.html`（theme-color 等 meta）
- Create: `public/icon.svg`（簡單撲克圖示：深色圓底 + 黑桃）、`e2e/offline.spec.ts`、`e2e/settings.spec.ts`
- Modify: `playwright.config.ts`（加 preview server project 或獨立 webServer 設定）

**規格：**
- `vite-plugin-pwa`：`registerType: 'autoUpdate'`、manifest（name「德州撲克練習」、theme/background 深色、icons 用 SVG `sizes: 'any'`）、precache 全部建置產物（預設 globPatterns 即可）
- 離線 e2e：preview webServer 的 command 必須是 **`npm run build && npm run preview -- --port 5200 --strictPort`**（單獨跑 e2e 時才不會用到殘留的舊 dist）；首次載入 → 等 SW controller 就緒（`navigator.serviceWorker.ready` + controller 非 null，必要時 reload 一次）→ `context.setOffline(true)` → reload → 「開始牌局」仍可見且可開桌打牌（**沿用 `?fastCpu=1`**）
- settings e2e（dev 5199）：改 CPU 數為 2 → 開桌 → seat 數 = 3；開關輔助資訊 → AssistPanel 顯示/消失
- **M3 全套驗收**：`npm run typecheck && npm run test && npm run coverage && npm run build && npm run test:e2e` 全綠

- [ ] **Step 1**: PWA 設定 + icon；`npm run build` 確認 SW 與 manifest 產出
- [ ] **Step 2**: e2e 兩個新 spec（offline 用獨立 playwright project + preview webServer）
- [ ] **Step 3**: 全套驗收指令全綠
- [ ] **Step 4**: Commit：`新增 PWA 離線支援與設定/離線煙霧測試`

---

## M3 驗收條件（對應 spec §13）

- [ ] 三種難度可選（開局設定）且行為可區分（normal 照表開牌、hard 會半詐唬）
- [ ] 練習輔助（勝率/賠率/成牌）可開關且預設開、設定持久化
- [ ] 手間補碼、破產重買、離開牌桌皆可用；結果橫幅 5 秒自動下一手
- [ ] 每手結束寫入 `holdem.hands`（環形 500）與 `holdem.stats`（counters）
- [ ] PWA：離線 reload 後仍可完整開桌打牌
- [ ] 全套指令全綠（typecheck / test / coverage ≥85 / build / e2e）
